import { PriceorderPipe } from './priceorder.pipe';

describe('PriceorderPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceorderPipe();
    expect(pipe).toBeTruthy();
  });
});
