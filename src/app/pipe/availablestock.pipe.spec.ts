import { AvailablestockPipe } from './availablestock.pipe';

describe('AvailablestockPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailablestockPipe();
    expect(pipe).toBeTruthy();
  });
});
